
<div class="travels">
  <h1><%= @travel.title %></h1>

  <%= cl_image_tag @travel.photo %>

  <section class="row travel">
    <div class="col-sm-8">
      <p>Ville: <%= @travel.destination %></p>
      <p>Durée: <%= @travel.duration %></p>
      <p>Mon Histoire: <%= @travel.story %></p>
      <p>Mes Annecdotes: <%= @travel.narrative %></p>
    </div>
  </section>

  <%= render "travels/button_list" %>
  <%= render "travels/button_list_favo" %>

</div>
